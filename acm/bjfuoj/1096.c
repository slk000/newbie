#include <stdio.h>
#include <memory.h>
int main(int argc, char const *argv[])
{
	int n;
	scanf("%d", &n);
	while(n--){

		int mi, i;
		scnaf("%d", &mi);
		int sum = mi*(mi+1)/2;
		int value[sum];
		for (i = 0; i < sum; ++i)
		{
			scanf("%d", value[i]);
		}

		for (i = 1; i < sum; ++i)
		{
			
		}

	}
	return 0;
}